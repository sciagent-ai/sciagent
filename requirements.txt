# Core dependencies
litellm>=1.0.0
requests>=2.28.0
flask>=2.0.0

# Web search (DuckDuckGo fallback when no Brave API key)
ddgs>=9.0.0

# Web content processing (for better HTML-to-text conversion)
html2text>=2024.2.26
beautifulsoup4>=4.12.0

# Optional: for code formatting tool
# black>=23.0.0

# Optional: for advanced JSON querying
# jmespath>=1.0.0
